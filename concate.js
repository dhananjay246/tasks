/*Create a function that takes multiple strings as input and concatenates them together using the
+ operator. Allow users to input strings dynamically and see the result instantly.*/

let str1="entre the string"
let str2= "entre the string"


function concatt(str1, str2)
  
{
  console.log("str1"+" "+"str2")
  
}